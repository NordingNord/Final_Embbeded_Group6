#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>
#include "matmul.hpp"
#include "img_identities.h"


int main() {

	//std::cout << "Testing nn with size: ";
	//std::cout << n_inputs << " x " << n_layer1 << " x " << n_layer2 << " x " << n_layer3 << std::endl;

	// //Load images
	// static float img_pixels[n_inputs];
	// std::string line;
	// std::ifstream pixel_file("img_pixel_vals.csv");
	// for (int i = 0; i < n_inputs; i++)
	// {
	// 	std::getline(pixel_file, line);
	// 	img_pixels[i] = std::stof(line);
	// }
	
	

	// int pred = nn_inference(img_pixels);
	// int act = img_characters[0];
	// std::cout << std::endl;
	// std::cout << 0 << ": NN Prediction: " << pred << " Should be: " << act;
	// if (pred != act) {
	// 	std::cout << " --Failed";
	// }
	// std::cout << std::endl;
	// std::cout << std::endl;

	return 0;
}
